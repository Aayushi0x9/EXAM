#include<stdio.h>
#include<string.h>

int main()
{
    char name[20], freq=1;

    printf("Enter any name : ");
    gets(name);

    for (int i = 0; name[i] != 'NULL'; i++)
    {
        
    }
    
    return 0;
}
